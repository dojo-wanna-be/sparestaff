<div class="circumstance-box">
  <div class="affect-poster">
    <span class="sub-title"><strong>Extenuating circumstances require documentation and include:</strong></span><br>
    <ul>
      <li>Serious illness</li>
      <li>Death of a family member</li>
      <li>Jury duty or other civil obligation</li>
      <li>Natural disaster</li>
      <li>Political unrest</li>
    </ul>
    If you have documentation, you may be eligible for a bigger refund. You can apply after you've cancelled.
  </div>
  <div class="hire_an_employee_box">
    <%= form_tag tell_poster_hiring_path(id: @transaction.id), method: :patch, id: "cancellation_reason_form" do |f| %>
      <%= hidden_field_tag "reason", "I have extenuating circumstance" %>

      <div class="my-mistake">
        <div class="title-main">Your Refund Summary</div>
        <div><b>This listing has cancellation policy</b></div>

        <div class="under-title">If you cancel now, you'll receive a full refund, minus Spare Staff service fees.</div>

        <div class="calculation-list">
          <div class="list-item">
            <div class="left-item">
              Total Fee
            </div>
            <div class="right-item">
              <%= "$#{(@transaction.amount + @transaction.service_fee).round(2)}" %>
            </div>
          </div>
          <div class="list-item">
            <div class="left-item">
              Service fee
            </div>
            <div class="right-item">
              <%= "-$#{@transaction.service_fee.round(2)}" %>
            </div>
          </div>
          <div class="list-item grey-list-item">
            <div class="left-item">
              <strong>Your Total Refund</strong>
            </div>
            <div class="right-item">
              <strong><%= "$#{@transaction.amount.round(2)}" %></strong>
            </div>
          </div>
        </div>

        <div class="custom-label">Tell <%= @listing.poster.name %> why you need to cancel</div>

        <%= text_area_tag "message_text" %>

        <%= submit_tag "Cancel Hiring", class: 'continue-btn' %>

        <div class="small-grey-text">Cancellation is effective immediately</div>
      </div>
    <% end %>
  </div>
</div>
