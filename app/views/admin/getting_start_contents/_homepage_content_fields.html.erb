<div class="nested-fields">
  <div class="field">
  	<%= f.select :section_type, options_for_select(HomepageContent.section_types.map {|k, v| [k.humanize.capitalize, k]}, f.object.section_type), {}, {class: 'custom-field'} %>
    <%#= f.select :section_type, options_for_select(HomepageContent.section_types.keys), {}, {class: 'custom-field half-field'} %>
  </div>
  <div class="field">
    <%= f.file_field :content_image %>
    <br/>
    <%= f.text_area :content %>
  </div>
  <%= link_to_remove_association "remove", f %>
</div>
